# MonaBit Frontend — Variables de entorno
# Copiar a .env y ajustar según entorno local

# Puerto del dev server (default: 3000)
VITE_PORT=3000

# URL base del backend API (default: http://localhost:4000)
VITE_API_URL=http://localhost:4000
